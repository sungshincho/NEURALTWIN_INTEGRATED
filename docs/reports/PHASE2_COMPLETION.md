# Phase 2 완료 리포트 — 디자인 통합 + DashboardLayout 업그레이드

> **커밋**: `e8a214c3`
> **브랜치**: `neuraltwin/sungshin`
> **완료일**: 2026-02-27
> **담당**: T4 (Website), T6 (Designer)

---

## 1. 변경 요약

| 태스크 | 설명 | 파일 수 | 상태 |
|--------|------|---------|------|
| P2-1: 디자인 토큰 통합 | `#00d4aa` 틸 액센트 CSS 변수 + Tailwind 설정 | 2 | 완료 |
| P2-2: DashboardLayout 업그레이드 | 헤더바, 매장 선택, 테마 토글, 모바일 탭바 | 1 | 완료 |
| P2-3: 공유 컴포넌트 정리 | useDarkMode 훅, sonner 토스트, GlassCard 통합 | 55 | 완료 |

**총 변경 파일**: 53개

---

## 2. P2-1: 디자인 토큰 통합

### 변경 사항
- `--accent`: `0 0% 42%` (모노크롬) → `168 100% 42%` (#00d4aa 틸) — 라이트/다크 모두
- `--accent-foreground`: `0 0% 5%` — 라이트/다크 모두
- `--sidebar-accent`: `168 100% 42%` — 라이트/다크 모두
- Tailwind `accent.hover`: `#00e8bb` 추가

### 파일
| 파일 | 변경 |
|------|------|
| `src/index.css` | CSS 변수 8개 업데이트, dark body 의사요소 추가 |
| `tailwind.config.ts` | accent.hover 추가 |

---

## 3. P2-2: DashboardLayout 업그레이드

### 데스크톱 레이아웃
```
┌─ Sidebar ──────┐┌─ Top Header ─────────────────┐
│ Logo            ││ [매장: 강남점 ▼] [🔔] [🌙] [👤] │
│ ● 인사이트 허브  │├───────────────────────────────┤
│ ○ 스튜디오      ││                               │
│ ○ ROI          ││  Content (Outlet)             │
│ ○ 설정         ││                               │
│ ────────       ││                               │
│ [User] [Logout] ││                               │
└────────────────┘└───────────────────────────────┘
```

### 모바일 레이아웃
```
┌─ Top Bar: [≡] [매장명] [👤] ─┐
│                               │
│  Content (full width)         │
│                               │
├─ Bottom Tab Bar ──────────────┤
│ [📊] [🎨] [💰] [⚙️]          │
└───────────────────────────────┘
```

### 추가 기능
- **매장 선택 드롭다운**: `useSelectedStore` Context 활용 인라인 컴포넌트
- **테마 토글**: Sun/Moon 아이콘, localStorage 연동
- **알림 벨**: 아이콘 (향후 실시간 연동)
- **모바일 사이드바**: 슬라이드인 + 배경 오버레이
- **모바일 탭바**: 4-탭 하단 네비게이션 (md:hidden)

---

## 4. P2-3: 공유 컴포넌트 정리

### 4-1. useDarkMode 훅 추출 (33개 파일)

**신규 파일**: `src/hooks/useDarkMode.ts`
```typescript
export function useDarkMode(): boolean {
  // MutationObserver로 document.documentElement.classList 감시
  // 'dark' 클래스 유무를 반응적으로 반환
}
```

**변경 파일**: 33개 컴포넌트에서 인라인 MutationObserver 코드 → `useDarkMode()` import로 교체

### 4-2. Toast 통일 — sonner (18개 파일)

**변경**: features/ 디렉토리 내 `useToast` (radix) → `toast` (sonner) 전환
- `import { useToast } from '@/hooks/use-toast'` → `import { toast } from 'sonner'`
- `toast({ title, description, variant })` → `toast.success(title)` / `toast.error(title)`

### 4-3. GlassCard 통합 (23개 파일)

**변경**: 인라인 GlassCard 정의 → `@/components/ui/glass-card` import
- `GlassCard` alias 추가 (기존 `GlassmorphicCard`와 호환)
- 23개 파일에서 중복 코드 제거

---

## 5. 디자인 시스템 통합 결과

| 항목 | Before | After |
|------|--------|-------|
| 액센트 컬러 | 모노크롬 (흑/백) | `#00d4aa` 틸 (HSL 168 100% 42%) |
| 다크모드 기본 | 라이트 | 다크 |
| useDarkMode | 33곳 인라인 복붙 | 1곳 공유 훅 |
| Toast | useToast + sonner 혼용 | sonner 단일화 |
| GlassCard | 3곳 인라인 정의 | 1곳 공유 컴포넌트 |
| DashboardLayout | 사이드바만 | 헤더바 + 매장 선택 + 모바일 탭바 |

---

## 6. 검증 결과

- `pnpm type-check`: 통과
- `pnpm build`: 통과
- 빌드 크기: 변화 없음 (코드 중복 제거로 미세 감소)

---

## 7. 다음 단계

- Phase 3: AI 챗봇 통합 + IoT 실시간 브릿지
  - P3-1: 세션 인계 (비회원→회원)
  - P3-2: OS AI 어시스턴트 사이드 패널
  - P3-3: IoT 실시간 데이터 연동
