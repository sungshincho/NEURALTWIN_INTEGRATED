# ══════════════════════════════════════════════════
# NeuralTwin 모노레포 — 루트 환경 변수
# ══════════════════════════════════════════════════
# Lovable AI Gateway 폐기됨 — 직접 API 연결 사용

# ── Supabase (전 서비스 공통) ──
SUPABASE_URL=https://bdrvowacecxnraaivlhr.supabase.co
SUPABASE_ANON_KEY=your_anon_key_here
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# ── AI Provider ──
AI_PROVIDER=google                          # 기본 AI 제공자: "google" 또는 "openai"
GOOGLE_AI_API_KEY=your_google_ai_api_key    # chat completion + embedding 모두 사용
OPENAI_API_KEY=                             # 선택, OpenAI fallback용

# ── 앱별 환경변수는 각 앱의 .env.example 참조 ──
# apps/website/.env.example
# apps/os-dashboard/.env.example
# apps/neuralsense/.env.example
