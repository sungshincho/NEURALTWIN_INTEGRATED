// Re-export from SelectedStoreProvider for backward compatibility
export { useSelectedStore } from "@/providers/SelectedStoreProvider";
export type { Store } from "@/providers/SelectedStoreProvider";
