# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
*.egg-info/

# Environment
.env
*.log

# Data files tracked via Git LFS (not ignored):
#   output/*.jsonl      — raw_rssi, zone_assignments, transitions, dwells, calibration, etc.
#   output/*.csv        — any exported CSVs
#   accuracy_tests/*.jsonl — per-phone accuracy test results
