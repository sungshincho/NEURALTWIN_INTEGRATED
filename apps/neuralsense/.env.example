# ============================================================
# NeuralSense Bridge — Environment Variables
# Copy this file to .env and fill in the values:
#   cp .env.example .env
# Then source before running:
#   export $(grep -v '^#' .env | xargs)
# ============================================================

# ── MQTT Broker (Tailscale VPN) ──
MQTT_BROKER_IP=100.87.27.7
MQTT_BROKER_PORT=1883
MQTT_TOPIC_PREFIX=neuralsense

# ── Raspberry Pi ──
PI_IDS=pi5,pi7,pi8,pi9,pi10,pi11,pi12,pi13

# ── Supabase Connection (REQUIRED) ──
SUPABASE_URL=https://bdrvowacecxnraaivlhr.supabase.co
SUPABASE_ANON_KEY=your_anon_key_here

# ── Store Configuration (REQUIRED) ──
STORE_ID=d9830554-2688-4032-af40-acccda787ac4
ORG_ID=0c6076e3-a993-4022-9b40-0f4e4370f8ef

# ── Upload Settings ──
# ENABLE_UPLOAD defaults to true in production.
# Set to false to run in local-only mode (no Supabase upload).
ENABLE_UPLOAD=true
UPLOAD_BATCH_SIZE=50
UPLOAD_INTERVAL_SEC=30
UPLOAD_MAX_RETRIES=3

# ── MAC Address Hashing (GDPR/Privacy) ──
# Set to true in production to hash MAC addresses before processing/storage.
# When enabled, all MAC addresses are SHA-256 hashed with the salt below.
# Same MAC + same salt = same hash (session linking still works).
NEURALSENSE_MAC_HASH_ENABLED=false
# IMPORTANT: Set a unique, secret salt in production. Keep it private.
# If unset, a default dev salt is used (NOT secure for production).
NEURALSENSE_MAC_SALT=your-secret-salt-here

# ── Data Output ──
OUTPUT_DIR=output
